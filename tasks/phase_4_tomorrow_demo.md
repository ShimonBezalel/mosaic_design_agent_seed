# Phase 4 — Tomorrow Demo

## Goal

Make the prototype usable in a meeting.

## Tasks

- Add a concise README usage section.
- Add `--interactive` mode if quick.
- Add pretty Markdown output.
- Add a `runs/` export directory ignored by git.
- Add one sample run artifact if explicitly requested.

## Acceptance

Yael can answer questions, rerun, and compare concept outputs in under 10 minutes.

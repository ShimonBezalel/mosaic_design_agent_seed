# Phase 0 — Repo Skeleton

## Goal

Create a Python package skeleton and validate sample JSON files.

## Tasks

- Add `pyproject.toml`.
- Add package `mosaic_agent/`.
- Add Pydantic models matching schemas.
- Add loaders for palette and brief.
- Add pytest setup.
- Add tests validating examples.

## Acceptance

- `pytest tests/test_schemas.py` passes.
